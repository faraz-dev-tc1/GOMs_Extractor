# GOMS Extraction API Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# ADK API Configuration (for agent-based processing)
# ============================================================================
# URL of the ADK API server
# Default: http://localhost:8000
ADK_API_URL=http://localhost:8000

# ============================================================================
# Google Cloud Storage Configuration (for automatic file uploads)
# ============================================================================
# GCS bucket name where processed files will be uploaded
# If not set, files will only be stored locally
# Example: my-goms-bucket
GCS_BUCKET=

# Path to Google Cloud service account JSON credentials
# If not set, will use default application credentials
# Example: /path/to/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=

# ============================================================================
# Google Cloud Project (for ADK and Vertex AI)
# ============================================================================
# Your Google Cloud project ID
# Required for ADK agent functionality
# Example: my-project-123456
GOOGLE_CLOUD_PROJECT=

# Google Cloud region for Vertex AI
# Default: us-central1
GOOGLE_CLOUD_REGION=us-central1

# ============================================================================
# API Server Configuration
# ============================================================================
# Host to bind the API server to
# Default: 0.0.0.0 (all interfaces)
API_HOST=0.0.0.0

# Port for the API server
# Default: 8080
API_PORT=8080

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Default: INFO
LOG_LEVEL=INFO

# ============================================================================
# Processing Configuration
# ============================================================================
# Default number of concurrent workers for PDF processing
# Adjust based on your system's CPU cores
# Default: 4
MAX_WORKERS=4

# Directory for temporary file uploads
# Default: /tmp/documents
UPLOAD_DIR=/tmp/documents

# ============================================================================
# Feature Flags
# ============================================================================
# Enable/disable automatic GCS upload
# If GCS_BUCKET is set, this is automatically enabled
# Values: true, false
# Default: true (if GCS_BUCKET is set)
# AUTO_UPLOAD_TO_GCS=true
